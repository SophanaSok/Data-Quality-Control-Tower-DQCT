# Copilot Instructions

- Treat `dqct.html` as the main build target.
- Keep changes focused and minimal.
- Preserve the existing folder structure under `dqct/`.
- Put rule configuration in `rules/ohio-buys.json`.
- Keep `schemas/` empty unless a schema is explicitly needed.
